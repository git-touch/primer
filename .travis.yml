language: dart
addons:
  apt:
    # Flutter depends on /usr/lib/x86_64-linux-gnu/libstdc++.so.6 version GLIBCXX_3.4.18
    sources:
      - ubuntu-toolchain-r-test # if we don't specify this, the libstdc++6 we get is the wrong version
    packages:
      - libstdc++6
before_install:
  - git clone https://github.com/flutter/flutter.git -b dev
  - ./flutter/bin/flutter doctor
  - ./flutter/bin/flutter config --enable-web
install:
  - ./flutter/bin/flutter pub get || true # TODO: example: no pubspec.yaml found
script:
  - echo success
before_deploy:
  - pub global activate flutter
  - pub global run flutterdoc build
deploy:
  local-dir: flutterdoc/build/web
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  committer-from-gh: true
  on:
    branch: master
