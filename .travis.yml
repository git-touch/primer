language: dart
before_deploy:
  - nvm install node
  - cd tool
  - npm install
  - npm start
  - cd ../primer-gallery
  - pub get
  - pub global activate webdev
  - webdev build
deploy:
  local-dir: primer-gallery/build
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  committer-from-gh: true
  on:
    branch: master
